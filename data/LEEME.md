Datos que cargan los scripts 02, 03 y 04 (formato Extended JSON).

equipos.json, jugadores.json y partidos.json se generan una sola vez desde
MongoDB Atlas con `npm run db:exportar` y SÍ deben subirse al repositorio:
son los que permiten reconstruir la base de datos en cualquier equipo.
